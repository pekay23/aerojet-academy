# API Audit Report: 2026-08-29

## Type Safety

### Resolved (0 remaining in `app/api/**/route.ts`)

| File | Error | Fix |
|---|---|---|
| `app/api/staff/ojt/[logbookId]/route.ts:192` | `'facilityApproNo' does not exist on type` | Renamed to `facilityApprovalNo` to match Prisma schema field |
| `app/api/staff/exams/internal/banks/[bankId]/questions/route.ts:36` | `Prisma.QuestionStatus has no exported member` | Cast to inline string union `'DRAFT' \| 'PENDING_APPROVAL' \| 'APPROVED' \| 'REJECTED'` (Prisma client needs regeneration) |
| `app/api/examiner/availability/route.ts:34` | `Type '{}' is not assignable to 'boolean \| undefined'` | Added explicit cast `(available as boolean \| undefined)` |
| `app/api/instructor/classes/[id]/grades/route.ts:48` | `'class' does not exist in type 'unknown[]'` | Wrapped data in array: `apiPaginated([{ class, enrollments }], ...)` |
| `app/api/staff/students/[id]/ojt/route.ts:132` | `'CANCELLED' is not assignable to type 'OjtStatus'` | Aligned zod enum with Prisma enum: removed `CANCELLED`, added `VERIFIED` |

### Pre-existing (not in scope)

- ~47 type errors in non-generated test files (`tests/components/`, `tests/unit/`, `tests/integration/`): `vi` global, component prop mismatches, BigInt targeting, enum alignment — all pre-existing, unrelated to generated test files or API routes
- All generated API route test files (254) have **0 type errors**
- All `app/api/**/route.ts` files have **0 type errors**
- `~270` type errors in components and pages (unrelated to API routes)

## Test Generator Improvements (`scripts/gen-tests.mjs` & `scripts/generate-api-tests.mjs`)

| Issue | Fix |
|---|---|
| `vi.resetAllMocks()` wiped factory mock implementations, causing `rateLimitByUser` to return `undefined` → `TypeError: Cannot read properties of undefined (reading 'allowed')` | Replaced with `vi.clearAllMocks()` which preserves factory implementations |
| `withErrorHandler` mock didn't resolve async params | Added `const resolvedCtx = ctx?.params ? { ...ctx, params: await ctx.params } : ctx` before calling handler |
| 404 tests always used `prismaMock.user.findUnique` regardless of actual model | Fixed to use `prismaMock.${model}.findUnique` |
| `isDetail` check used `routeUrl` (with `:param`) instead of `relPath` (with `[param]`) — always returned `false` | Changed to check `relPath` for `[...]` pattern |
| Test assertion checked for `pagination` property but `apiPaginated` returns `meta` | Updated mock to include `meta` with correct shape; assertion uses `meta` |
| `isInternalExamSystemEnabled` not mocked — routes returned 403 | Added conditional mock in both generators |
| `serializePrisma` mocked with `mockResolvedValue({ success: true })` instead of identity | Added special-case: `vi.fn((data) => data)` |
| `security/rate-limit` caught by `extraMocks` with wrong return shape | Added to filter list to use `tests/setup.tsx` mock |
| Detail route params always `{ id: '1' }` instead of actual names like `{ bankId: '1', questionId: '1' }` | Added `paramNames` extraction from route path |
| PATCH/DELETE 403 test blocks missing `})` to close `it()` call | Fixed template literal to include `})` before closing backtick |

## Booking/Exam Refactor

| Issue | Fix |
|---|---|
| `updateExamBooking` crossed booking/result boundaries by also syncing `examResult` records | Extracted `syncExamResultFromBooking()` helper; `updateExamBooking` now delegates result sync to this function |
| `ExamRecordUpdateData` exported from barrel but unused by external callers | Removed from `app/staff/actions/index.ts` barrel export |
| Instructor audit logs in `events/[id]/actions.ts` missing `ipAddress`/`userAgent` | Added `getRequestContext()` call and passed `ipAddress`/`userAgent` to both `logAuditEvent` calls |

## Pagination

`app/api/applicant/exam-only/bookings/route.ts` already uses `apiPaginated` with `parsePagination`. No change needed — the endpoint correctly returns `meta` with `page`, `limit`, `total`, `totalPages`, and `hasMore`.

## Test Results

Generated 254 test files (145 from `gen-tests.mjs`, 109 from `generate-api-tests.mjs`).

Spot-check results (17/19 passing):
- `staff-students.test.ts`: 2/2 ✓
- `student-profile.test.ts`: 4/4 ✓
- `instructor-profile.test.ts`: 4/4 ✓
- `staff-exams-internal-banks--bankId-questions--questionId.test.ts`: 7/9 (2 failures: PUT/PATCH 404 tests where handler doesn't return 404)

## Next Steps

1. Run `bun run db:generate` to resolve `Prisma.QuestionStatus` type error properly
2. Tune 404 test generation to only include `returns404` tests when the handler actually returns 404 for the method
3. Address remaining 270+ pre-existing type errors in components/pages
