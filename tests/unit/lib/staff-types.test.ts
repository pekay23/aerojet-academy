import { describe, it, expect } from 'vitest'
import * as StaffTypes from '@/lib/staff/types'

describe('lib/staff/types', () => {
  it('exports ApprovalResult interface', () => {
    expect(StaffTypes).toBeDefined()
  })

  it('exports ApprovalOptions interface', () => {
    expect(StaffTypes).toBeDefined()
  })
})
